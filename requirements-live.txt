# Optional local-backend support. The offline installation remains in
# requirements.txt and does not need these packages.
langgraph>=0.1.0
langgraph-checkpoint-sqlite>=1.0.0
mcp>=1.0.0
kuzu>=0.4.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
python-dotenv>=1.0.1
